# Project Progress

## 2026-06-27

- Created seven architecture notes:
  - `01_sensory_streams_system.md`
  - `02_threshold_monitors.md`
  - `03_importance_gate.md`
  - `04_memory_promotion.md`
  - `05_inner_world_model.md`
  - `06_habit_builder.md`
  - `07_efficiency_enhancer_model.md`
- Completed the 30-question design interview for `01_sensory_streams_system.md`.
- Recorded the current `01` target: create 10 Markdown sensory sample sheets in `sensory_stream_samples/`.
- Locked the first sample-sheet shape: 20 ticks per sheet, five values per tick: `brightness`, `volume`, `touch`, `taste`, and `smell`.
- Clarified the `01` boundary: the sensory stream system only emits/clamps temporary input and exposes the current rolling buffer; threshold, importance, and memory decisions belong to later layers.
- Completed the 30-question design interview for `02_threshold_monitors.md`.
- Recorded the current `02` boundary: threshold monitors are deterministic trigger recorders, similar to spreadsheet formulas. They record threshold hits only; they do not decide importance, gate opening, memory storage, or emotional meaning.
- Locked the first `02` constants: `n` threshold hits when a current sensory value equals `1.0`; `n-1` rate-of-change hits when change is at least `0.5`.
- Clarified `n`: current tick only, no previous-tick comparison.
- Clarified `n-1`: detects rate of change in two ways, within the same sensor and between different sensors.
- Clarified initial `n-2`: measures acceleration of change by reading the populated `n-1` rate-of-change trigger sheet.
- Recorded the current `02` target: after the 10 sensory sample sheets exist, create one Markdown threshold trigger sheet per sensory sample sheet in `threshold_trigger_sheets/`.
- Started `03_importance_gate.md` planning, then paused before recording answers because the importance gate needs real `01` and `02` output examples first.
- Created 10 sensory sample sheets in `sensory_stream_samples/`.
- Each sensory sample sheet contains 20 ticks and five normalized values per tick: `brightness`, `volume`, `touch`, `taste`, and `smell`.
- Created 10 matching threshold trigger sheets in `threshold_trigger_sheets/`.
- The trigger sheets apply the locked `02` rules: `n` records `threshold_hit` when a value equals `1.0`; `n-1` records within-sensor and between-sensor rate-of-change hits when one-tick absolute change is at least `0.5`.
- Early result from the concrete sheets: quiet and soft samples produce no trigger rows, isolated spike samples produce a small number of clear rows, and noisy samples produce many rows. This gives `03_importance_gate.md` better evidence for deciding what to ignore, watch, promote, record, or escalate.
- Clarified that the first test does not need to be a live stream. It can be a connected workbook with separate sheets for sensory input, `n`, `n^-1`, `n^-2`, and a review/gate sandbox.
- Created `outputs/connected_spreadsheet_test/connected_sensory_threshold_gate_test.xlsx` as the first connected spreadsheet prototype.
- Reworked the connected workbook to be compact and numeric-first: the first tab is `00 Compact View`, sensory values stay `0.0` to `1.0`, and threshold outputs use `0` or `1` instead of word labels.
- Recorded an initial memory-storage idea in `04_memory_promotion.md`: the colored brain areas can map to five sensory storage pools, starting at 20 GB each for a 100 GB total, with later reallocation by the mind model based on use, need, and survival relevance.
- Recorded an initial inner-world storage idea in `05_inner_world_model.md`: the mind gets its own separate 100 GB storage budget for building an internal world model, with an initial 80% inner-world / 20% outside-world attention split.
- Populated `03_importance_gate.md` with a first-pass gate design: emergency/reflex route, attention/brain route, three adjustable duration knobs, resource pressure rules, and gate outputs.
- Expanded `04_memory_promotion.md` with promotion inputs, memory forms, storage-pressure behavior, and sensory-pool reallocation signals.
- Expanded `05_inner_world_model.md` with the mind's world-building role, possible visual/video-game-like imagination representation, decision-tuning pressures, resource body state, and relationship to the importance gate.
- Populated `06_habit_builder.md` with a first-pass habit shape, habit promotion rules, context requirements, and relationship to the efficiency enhancer.
- Populated `07_efficiency_enhancer_model.md` with mature-habit compression targets, resource-pressure behavior, and a safety rule for reopening failed shortcuts.
- Refined the gate/mind relationship: threshold constants, duration windows, route weights, attention split, storage pressure, and compression pressure should be exposed as selectable knobs/tools that the mind model can manipulate.
- Recorded the current decision pressure model: physical limits constrain available choices, while innate drives such as self-preservation, curiosity, stability, efficiency, and coherence help the mind model choose what to adjust.
- Completed a 30-question mind-model tools/knobs interview and recorded answers in the relevant architecture logs: threshold monitor knobs, importance gate controls, memory promotion tools, inner-world/resource controls, habit automation thresholds, and efficiency compression tools.
- Clarified a key design pattern from the interview: many controls should be visible to the mind model but operate homeostatically or deterministically in the background, such as sensory storage reallocation, memory/pathway strengthening and decay, and habit automation pressure.
- Decided the next implementation step should be a compact spreadsheet control-surface prototype, not n8n yet. The spreadsheet is better for seeing knobs, resource meters, mode presets, and gate formulas all at once; n8n is better later after the control logic is proven and ready to become an event workflow.
- Next work: extend the existing connected workbook with `03`/mind-control layers: editable mind knobs, resource body meters, global mode presets, and numeric gate outputs such as ignore, watch, promote, episode, emergency, and send_upward.
- Extended `outputs/connected_spreadsheet_test/connected_sensory_threshold_gate_test.xlsx` with the first compact control-surface prototype: `03 Mind Knobs`, `04 Resource Body`, `05 Gate Output`, `06 Mode Presets`, and renamed the sample-level rollup to `07 Summary`.
- The new workbook keeps formulas inspectable and numeric-first: mind knobs expose editable mode/novelty/emergency/window/resource/curiosity/attention controls, resource body tracks storage/RAM/heat against the `0.80` marker plus deterministic sensory GB reallocation, and gate output reports `ignore`, `watch`, `promote`, `episode`, `emerg`, and `up` routes as `0`/`1`.
- Completed the 20-question control-surface interview in `08_control_surface_interview.md`.
- Implemented the interview conclusions into the architecture docs: curious startup defaults, `50%` inner-world building plus `30%` curiosity default allocation, danger-mode outward attention, deterministic protective reflexes, equal per-sense emergency priority, trigger-sequence-reward habit handoff, aggressive efficiency pressure, compact routine audit trails, disuse-based access/detail decay, and homeostasis as resource-danger prevention.
- Added a future architecture note in `05_inner_world_model.md`: later passes should consider two or more cooperating mind models for internal dialogue, reflection, planning, self-checking, creativity, and decision quality.
- Reworked the connected workbook formulas around the Q20 priority: better homeostasis, habit handoff, and efficiency pressure. The regenerated workbook now includes a compact `06 Habit Efficiency` sheet, homeostasis/resource-danger fields in `04 Resource Body`, and numeric handoff/automation/audit outputs in the sample summary.
- Chose the v1 cooperative mind-model direction: two mind models, Builder / Dreamer and Critic / Reality-Checker. Inner-world building can use Scene Builder, Memory Mapper, and Future Simulator layers as tools of the Builder, not as a third full mind model yet. A later Narrator / Integrator remains a future option if two models become too limited.
- Clarified the habit model's conversation boundary: it should not join Builder/Critic dialogue as a third mind by default. It should quietly observe trigger, sequence, and reward in order, promote reliable loops deterministically, hand mature loops to efficiency, and report upward only for new candidates, risky handoffs, failures, surprises, context mismatches, or protected areas.
- Defined the desired habit/efficiency output as a learned operation control: a deterministic code-based button, dial, tool, or routine that appears on the Builder/Critic control surface after a mature habit is compressed, so the mind models can use the operation later without thinking through the whole process.
- Froze the threshold monitor as good enough for the v1 baseline: existing knobs are sufficient, the layer should stay deterministic/formula-like and inspectable, and adaptive learned thresholds should wait until false alarms, missed events, useful promotions, and gate outcomes create real learning evidence.
- Clarified the importance gate's physical implementation direction: deterministic preset global recording/attention dial positions such as calm, curious, focused, strained, danger, and recovery, selectable by the two mind models while always balancing storage, RAM/working memory, heat/compute, and power constraints.
- Limited the habit/efficiency pipeline's write power: learned controls must be proposed through `learned_operation_controls.md`, active buttons are capped at 12 for v1, new proposals are capped at 3 per review cycle, and every proposed button/dial/tool/routine must use the same structured control template before Builder / Dreamer and Critic / Reality-Checker can accept it.
- Froze the v1 executive tool surface as sufficient: Builder / Dreamer and Critic / Reality-Checker now have enough knobs, preset dials, resource meters, evidence controls, habit/efficiency pressure, limited learned operation controls, and inner-world tools to test selection behavior before adding more executive controls.
- Created `09_end_to_end_scenario_tests.md` with five walkthrough tests: isolated volume spike, cross-sense impact, repeated touch danger, dense noise under resource strain, and repeated cleanup loop becoming a proposed learned control.
- Ran the first numeric scenario test with `scenario_tests/run_scenario_01_isolated_volume_spike.mjs`; the test report is `outputs/scenario_tests/scenario_01_isolated_volume_spike_result.md`. Result: FAIL in a useful way. The gate avoids emergency routing but promotes an isolated one-sense spike into an episode because `n^-2` acceleration is too strong without cross-sense support.
- Adjusted the scenario test formula so `n^-2` acceleration can produce `watch` or `send_upward` but cannot create an episode alone. Episode now requires promotion plus cross-sense support, repeated evidence, emergency/protective relevance, or explicit Builder / Critic confirmation. The first three numeric trials now pass: isolated volume spike, cross-sense impact, and repeated touch danger.
- Clarified full per-sensor recording policy: compact threshold triggers are cheap notices, but full sensor clips are saved only when Builder / Dreamer and Critic / Reality-Checker open a recording window through the current gate preset, curiosity/emergency settings, and resource constraints. Added `sensor_recordings/` with per-sensor folders tied to the default 20 GB sensory storage pools.
- Simplified the raw recording controls to exactly five executive buttons, one per sensor: `record_brightness`, `record_volume`, `record_touch`, `record_taste`, and `record_smell`. Each button toggles raw recording for its own sensor on or off; there is no duration, budget, cost, or all-sensor recording control in v1.
- Clarified that `n^-2` compact monitoring is always on. Raw sensor recording buttons only control full raw sensory clips; they do not turn off `n`, `n^-1`, or `n^-2`, and `n^-2` continuously watches the compact trigger stream for second-order change.
- Set up the concrete v1 recording control surface in `sensor_recordings/recording_controls.md` and added `scenario_tests/run_recording_button_control_test.mjs`. The generated report `outputs/scenario_tests/recording_button_control_result.md` passes: `record_volume` toggles raw recording on/off while `n^-2` continues firing whether raw recording is on or off.
- Set up `habit_model/` as the habit model workspace, with `mind_model_logs/` for compact Builder/Critic logs, `trigger_sequence_reward_logs/` for extracted loop rows, and `candidate_habits/` for repeated loops that may later be proposed through `learned_operation_controls.md`.
- Updated implementation order: build one executive mind model first, using the existing instruments and five sensor recording buttons. The Builder / Dreamer and Critic / Reality-Checker split remains a later architecture step after the single executive can reliably read instruments, choose controls, and write useful compact decision logs. Serious habit-model extraction should wait until that single executive logging path works.
- Added the first deterministic streaming-layer test in `scenario_tests/run_streaming_layer_test.mjs`.
- The streaming test generates 20 artificial world ticks for brightness, volume, touch, taste, and smell, appends normalized values to `outputs/streaming_world/session_001_stream_log.md`, rewrites a capped 10-tick buffer at `outputs/streaming_world/session_001_rolling_buffer.md`, and writes compact `n`, `n^-1`, and `n^-2` trigger rows to `outputs/streaming_world/session_001_compact_trigger_log.md`.
- Added concrete raw recording button behavior to the streaming test: when a button turns on, the recorder copies that sensor's current rolling-buffer rows into the sensor's raw file, then appends new rows while recording remains on.
- The generated report `outputs/streaming_world/streaming_layer_test_result.md` passes. It verifies that the rolling buffer drops old rows, compact monitoring stays on across the whole stream, and raw sensor files are populated only when the matching recording button copies/appends rows.
- Added the first longer constant-stream model experience run in `scenario_tests/run_constant_stream_experience.mjs`.
- The experience run sends 60 deterministic artificial ticks through the system, turns on all five sensor recording buttons at tick 1, keeps a capped 10-tick rolling buffer, writes compact trigger rows, and creates a readable noticed log at `outputs/model_experience/experience_session_001_noticed_log.md`.
- The generated summary `outputs/model_experience/experience_session_001_summary.md` passes. It shows 60 ticks, final buffer ticks 51-60, 17 noticed ticks, 3 `n` triggers, 20 `n^-1` triggers, and 15 `n^-2` triggers.
- Logged the end-goal perception constraint: the executive model should see the world primarily through compact `n`, `n^-1`, and `n^-2` logs, not direct raw stream access.
- Clarified that the reason for `n`-first perception should be physical pressure, not just a written rule. Raw recording, raw inspection, and raw analysis should consume enough storage, RAM/working memory, processor load, heat, and power that the system must use compact logs roughly 80% of the time or more to avoid overload.
- Added the first resource-enforced perception test in `scenario_tests/run_resource_pressure_perception_test.mjs`.
- The generated report `outputs/resource_pressure/resource_pressure_session_001_result.md` passes. It compares a compact-first executive with a raw-heavy executive on the same 60-tick stream, using explicit storage, RAM/working-memory, compute, heat, and power meters.
- Result: the compact-first executive reads compact logs on 100% of ticks, opens raw detail on 7 justified ticks, and stays below the 80% danger marker with max pressure at 55.56%. The raw-heavy executive requests raw detail on 100% of ticks, is allowed raw detail on only 8.33% of ticks before forced fallback, hits 93.8% max pressure, and falls back to compact perception 50 times.
- This gives the first inspectable proof that compact logs are not merely preferred by policy; they are the only perception channel cheap enough to keep open continuously while raw logs remain selective tools.
- Logged the compact-shape interpretation rule: the model can often infer what happened from `n`, `n^-1`, `n^-2`, source sense, and context without opening raw logs.
- For touch, rate-shape can separate gradual heat/pressure, hot-stove contact, and sharp-object contact. Raw data should be reserved for ambiguity, high stakes, or correction/training detail rather than normal perception.
- Important compression checkpoint: in the 100-chunk kitchen batch, the hidden full dataset is 91,067 bytes / 1,415 lines, while the compact `n` log is 7,202 bytes / 106 lines. The compact log is about 7.9% of the full stream size, roughly 12.6x smaller by bytes and 13.3x fewer lines.
- Despite that compression, the compact log still catches all 20 hot-stove events and produces no false alarms across the 80 ambient chunks in the clean baseline. This is the first strong evidence that compact perception can preserve useful signal while discarding most raw stream volume.
- Logged the monocular sight constraint: with one visual input, the model cannot rely on stereo parallax. It should build rough location from movement over time, sight rate changes, selective visual anchor snapshots, touch anchors, volume/echo changes, smell gradients, and action history.
- Logged the mental-map principle: the model's most human-like resource is a useful inner world containing places, objects, risks, expectations, opinions, and predictions. Raw data is training material and occasional clarification; the mental map is the operating asset that lets the model interpret compact logs cheaply.
- Logged the bootstrapping caveat: resource limits and tools may not make inner-world building emerge by themselves at first. Early scheduled/background routines can summarize compact logs, revise map entries, record corrections, and generate labeled examples until world-building becomes a learned habit.
- Refined the bootstrap plan: early cron-like routines can be deterministic handlers for specific compact `n` log patterns, asking map-building questions, updating anchors, recording corrections, and marking when raw detail was or was not useful.
- Added a delayed consolidation idea: about one minute after a notable event, a cron-like handler can ask whether the event should become a reusable general rule, separating immediate response from later rule formation.
- Added the first deterministic inner-world bootstrap scenario in `scenario_tests/run_inner_world_bootstrap_hot_stove.mjs`.
- The generated result `outputs/inner_world_bootstrap_hot_stove/hot_stove_inner_world_bootstrap_result.md` passes. It feeds only compact hot-stove `n`, `n^-1`, and `n^-2` rows into cron-like handlers, then writes a map-update log, trusted teacher correction log, one-minute delayed consolidation log, and reusable-rule output.
- First bootstrap result: touch `n=1` in the kitchen/stove area asks what surface is here; touch rate-shape creates a possible hot-surface risk hypothesis; teacher correction confirms `touched_hot_stove`; the delayed handler approves a reusable rule candidate that normally interprets similar future events from compact logs plus map context without opening raw detail.
- Reviewed the first bootstrap scenario in `05_inner_world_model.md`: it is a useful scaffold proof but still a happy-path test. The next pressure test should use three cases: real hot stove, harmless high-pressure counter touch, and sharp object contact, so the system must avoid overgeneralizing one compact touch pattern into every touch danger.
- Added the three-case overgeneralization test in `scenario_tests/run_inner_world_bootstrap_three_case_touch.mjs`.
- The generated result `outputs/inner_world_bootstrap_three_case_touch/three_case_inner_world_bootstrap_result.md` passes. It compares real hot-stove contact, harmless counter pressure, and sharp-object contact using compact touch rows plus signed touch-change shape.
- Three-case result: real hot-stove contact creates exactly one hot-surface rule; harmless counter pressure updates the inner-world map as a contact anchor but creates no danger rule; sharp-object contact creates a separate sharp-object rule. The run also writes `three_case_inner_world_map_state.json` so future tests can reload structured map state instead of only reading logs.
- Recorded the caregiver-rule framing for early cron routines: these are structured good ideas, like parents teaching children to look both ways before crossing a road. They scaffold useful safety and map-building behavior until enough correction and repetition make the behavior learned rather than manually prompted.
- Refined robot sensory input organization: sight/brightness, hearing/volume, taste, and smell can stay as one coarse stream each for now, but touch should split into multiple body-location streams because contact meaning depends heavily on where it happens.
- Added a first 10-location robot touch layout: left/right fingertips, left/right palms, left/right forearms, front/back torso, and left/right foot/base.
- Added `scenario_tests/run_multi_touch_body_sensor_layout.mjs`. The generated result `outputs/multi_touch_body_sensor_layout/multi_touch_body_sensor_layout_result.md` passes: compact logs preserve body-location names, distinguish fingertip contact from foot/base contact, make torso collision a different map question, and detect two-palm grip as a between-touch-location event.
- Added `scenario_tests/run_road_crossing_caregiver_rule.mjs` as the next independent whole-system bootstrap test using the multi-location touch layout.
- The generated result `outputs/road_crossing_caregiver_rule/road_crossing_caregiver_rule_result.md` passes. It tests compact sight/volume/movement/touch-location evidence feeding a caregiver-style road-crossing routine: street-edge compact evidence plus trusted teacher correction creates one reusable crossing rule, while a sidewalk obstacle and driveway vehicle-sound caution become narrower map updates and do not overgeneralize into the crossing rule.
- Road-crossing result: early deterministic routines can scaffold parent-style safety behavior from compact logs, but delayed consolidation only approves a reusable rule when compact evidence, map context, and teacher correction agree.
- Logged the non-visual navigation and superhuman-sensor principle: blind navigation shows that a usable spatial inner world can come from cane/contact vibration, echo, footstep sound, air movement, smell, slope, timing, memory, and correction rather than vision alone.
- Extended the sensory architecture with future superhuman streams such as ultrasonic echo, infrared heat, lidar-like distance pulses, floor vibration, airflow/pressure, magnetic/electrical field shifts, and chemical traces. These can all feed the same compact `n` event vocabulary instead of requiring separate raw-thought systems.
- Added `scenario_tests/run_non_visual_superhuman_spatial_inference.mjs` as the next independent whole-system test for non-visual/superhuman spatial inference.
- The generated result `outputs/non_visual_superhuman_spatial_inference/non_visual_superhuman_spatial_inference_result.md` passes. It tests ultrasonic echo, reflection volume, movement history, weak visual evidence, and front/foot body-risk signals as compact logs, not raw vision.
- Non-visual spatial result: confirmed obstacle and close low-contrast obstacle cases support one reusable obstacle-distance rule; open-space echo and moving sound-source cases stay narrower map anchors and do not overgeneralize into physical obstacle beliefs.
- Logged the labeled touch/pressure differential principle: paired or body-distributed touch sensors can potentially support learned gravity, balance, equilibrium, airflow, and barometric/atmospheric pressure inference when fused with movement history and other compact senses, reducing the need for dedicated early accelerometer, balance, or pressure thought systems.
- Added `scenario_tests/run_body_pressure_balance_inference.mjs` as the next independent whole-system test for labeled touch/pressure differential body-state inference.
- The generated result `outputs/body_pressure_balance_inference/body_pressure_balance_inference_result.md` passes. It tests left/right foot pressure, torso contact, paired pressure capsule, movement command, and airflow as compact logs, with no raw accelerometer, barometer, or equilibrium fields.
- Body-pressure result: the confirmed left-tilt case supports one reusable balance/gravity rule, while local foot obstacle, commanded forward acceleration, and airflow pressure-gradient cases remain narrower anchors and do not overgeneralize into balance beliefs.
- Added `10_digital_embodied_world.md` to define the next major phase: a small hidden digital world where the robot has a body, receives only compact logs, and must build an inner map rather than reading simulator truth.
- Logged the body-noticing cron idea: early caregiver-style routines should periodically ask what the robot's own pressure, touch, posture, movement, collision, and prediction errors say about its body state, so it can build proprioception-like self-awareness while navigating.
- Logged the movement-urge cron idea for the single-executive phase: when the model is stuck or over-waiting, a guarded cron can urge small reversible actions that generate new compact evidence, while yielding to danger, unstable balance, uncertainty, teacher correction, and resource pressure.
- Completed the 25-question digital embodied physics-world interview in `digital_embodied_world_physics_interview.md`.
- Chose the first world as a tiny continuous 2D physics nursery: simple custom felt physics, about 10 x 10 units, one small box-body robot, wall boundary, low obstacle, slope/pressure patch, fan/airflow source, and edge/curb warning zone.
- Refined the cron architecture for the physics nursery: every intentional action gets a prediction-before-action record, every action gets immediate comparison-after-action plus delayed settling checks when needed, and a separate compact-surprise cron handles sudden `n`, `n^-1`, `n^-2`, or cross-sensor changes.
- Added `11_tiny_physics_nursery_plan.md` as the written implementation target, including hidden simulator truth boundaries, compact sensor streams, action set, cron loop, output files, and pass conditions for run-2 improvement over run 1.
- Built the first tiny digital embodied world in `digital_world/`: `physics_nursery_world.mjs` defines the hidden 10 x 10 felt-physics nursery, `physics_nursery_sensors.mjs` converts hidden state into compact `n`/`n^-1`/`n^-2` body/world streams, and `physics_nursery_crons.mjs` handles prediction, comparison, compact surprise, body-noticing, movement urge, and lesson candidates.
- Added `scenario_tests/run_tiny_physics_nursery.mjs`, which runs the robot through the same nursery twice. Run 1 is a naive scripted pass; run 2 uses compact lessons to probe before wall, low-obstacle, and curb/edge commitments.
- Generated `outputs/tiny_physics_nursery/` with separate hidden truth, compact trigger, prediction/comparison, body/world map update, lesson candidate, result, watcher data, and visual watcher files.
- The tiny nursery result passes: run 2 reduces torso-front contacts from 1 to 0, reduces blocked/partial/warning full steps from 3 to 0, raises probe warnings before risky commitment from 0 to 3, and improves prediction accuracy from 63.6% to 75.0%.
- Verified the robot-facing nursery logs do not include hidden coordinates or simulator feature objects; hidden truth stays in the evaluation/watch files.
- Replaced the fixed run-2 action script with `digital_world/physics_nursery_action_chooser.mjs`, a small transparent compact-belief executive. The chooser reads prior compact-derived body/world updates plus current compact outcomes, then selects `probe_forward`, turns, `recenter_body`, `pause`, or `step_forward` with a logged reason.
- Added `outputs/tiny_physics_nursery/action_decision_log.md` and updated the watcher sidebar so run 2 is labeled as the compact chooser pass. The autonomous run still passes: torso-front contacts 1 -> 0, blocked/partial/warning full steps 3 -> 0, probe warnings before risky commitment 0 -> 3, and prediction accuracy 63.6% -> 66.7%.
- Built the next incremental 2.5D embodied nursery in `digital_world/`: `physics_2_5d_nursery_world.mjs` adds a flat-floor path with height-like physical consequences for ramp load shift, ledge/drop warning, low overhead clearance, and raised pressure surface; `physics_2_5d_nursery_sensors.mjs` converts those hidden features into compact streams such as `height_pressure`, `foot_drop_warning`, `overhead_clearance`, `vertical_echo`, `body_pitch_pressure`, and `ramp_load_shift`.
- Added `digital_world/physics_2_5d_action_chooser.mjs`, a transparent compact-belief executive that can choose `probe_forward`, `crouch_body`, `recenter_body`, `pause`, `stand_body`, or `step_forward` from compact-derived beliefs such as `rampRisk`, `dropRisk`, `overheadRisk`, and `raisedSurfaceRisk`.
- Added `scenario_tests/run_tiny_2_5d_nursery.mjs` and generated `outputs/tiny_2_5d_nursery/` with hidden truth, compact trigger log, prediction/comparison log, body/world updates, lesson candidates, action decision log, watcher frames, and `tiny_2_5d_nursery_watcher.html`.
- The 2.5D nursery result passes: committed height warnings 6 -> 2, overhead contacts 4 -> 0, probe height warnings before commitment 0 -> 3, crouch actions 0 -> 1, prediction accuracy 40.0% -> 100.0%, and unresolved prediction mismatches 6 -> 0.
- Verified the compact-only perception boundary for the 2.5D nursery: robot-facing logs omit hidden coordinates and simulator feature objects; hidden `true x`/`true y` and feature names stay in the evaluation/watch artifacts.
- Scaled the 2.5D work one step wider without jumping to full 3D: `physics_2_5d_nursery_world.mjs` now supports a second layered room layout through `LAYERED_ROOM_2_5D` and configurable initial state while preserving the original tiny 2.5D room.
- Added `scenario_tests/run_layered_2_5d_nursery.mjs`, which trains on compact-derived lessons from the first 2.5D room, then compares a naive pass and a compact-transfer chooser pass in a second room with different feature placement.
- Generated `outputs/layered_2_5d_nursery/` with hidden truth, compact trigger log, prediction/comparison log, body/world updates, lesson candidates, action decision log, watcher frames, and `layered_2_5d_nursery_watcher.html`.
- The layered 2.5D transfer result passes: committed height warnings 5 -> 1, overhead contacts 4 -> 0, probe height warnings before commitment 0 -> 4, crouch actions 0 -> 1, prediction accuracy 41.7% -> 100.0%, and unresolved prediction mismatches 7 -> 0.
- Re-ran the earlier tiny 2.5D and original 2D nursery tests after the shared world/chooser changes; both still pass.
- Added `digital_world/physics_2_5d_risk_memory_chooser.mjs`, replacing fixed route timing with short-lived compact risk memory such as `overheadAhead`, `dropAhead`, `rampLoadAhead`, `raisedSurfaceAhead`, `crouchedForClearance`, and `settledRaisedSurface`.
- Added `scenario_tests/run_adaptive_2_5d_nursery.mjs`, a third 2.5D room with overhead risk first, then raised surface, drop/ledge, and ramp/load pressure. The adaptive chooser reads compact sensor/risk memory only and chooses probes, crouch, recenter, pause, or steps without hidden coordinates or fixed route step thresholds.
- Generated `outputs/adaptive_2_5d_nursery/` with hidden truth, compact trigger log, prediction/comparison log, risk memory log, body/world map updates, action decision log, habit candidates, watcher frames, and `adaptive_2_5d_nursery_watcher.html`.
- The adaptive 2.5D result passes: committed height warnings 6 -> 2, overhead contacts 4 -> 0, probe warnings before commitment 0 -> 3, crouch actions 0 -> 1, prediction accuracy 41.7% -> 100.0%, unresolved prediction mismatches 7 -> 0, and habit candidates 0 -> 1.
- Added the first candidate learned operation control to `learned_operation_controls.md`: `adaptive_low_clearance_crossing_routine_v1`, triggered by compact overhead risk memory and shaped as `probe_forward -> crouch_body -> step_forward`.
- Re-ran the layered 2.5D, tiny 2.5D, and original 2D nursery tests after the adaptive risk-memory addition; all still pass.
- Added quiet-probe decay to `physics_2_5d_risk_memory_chooser.mjs`: if `probe_forward` does not confirm overhead or drop risk, the short-lived `overheadAhead`/`dropAhead` memory clears instead of looping forever on a false alarm.
- Added `scenario_tests/run_habit_promotion_2_5d_nursery.mjs`, which reviews `adaptive_low_clearance_crossing_routine_v1` across five cases: normal low tunnel, early low tunnel, tunnel after raised surface, side-echo false alarm, and too-low-even-crouched failure.
- Generated `outputs/habit_promotion_2_5d_nursery/` with compact trigger log, action decision log, risk memory log, promotion review, hidden truth log, and result report.
- The habit promotion review passes and recommends `proposed`: intended success cases 3/3, false alarm avoided, too-low failure caught, and confidence `medium_high`.
- Updated `learned_operation_controls.md` so `adaptive_low_clearance_crossing_routine_v1` is now `status: proposed`, with audit evidence from the promotion test.
- Added `digital_world/embodied_agent_tools.mjs`, the first model-ready tool boundary for the embodied nursery. It exposes `readCompactSensors()`, `readRiskMemory()`, `predictAction(action, reason)`, `chooseAction(action, reason)`, `writeMapBelief(...)`, `useLearnedControl(control_id)`, and `requestRawDetail(reason)` while keeping hidden simulator truth out of agent-facing returns.
- Added `scenario_tests/run_embodied_agent_tools_boundary.mjs`, which runs the current deterministic risk-memory policy through the same tool surface a future AI model would call.
- Generated `outputs/embodied_agent_tools/` with agent-facing action log, risk memory log, map beliefs, raw-detail denials, tool audit log, hidden truth log, and result report.
- The embodied agent tool-boundary result passes: zero overhead step contacts, 3 probe warnings, 1 crouch action, raw detail denied, proposed learned control denied until active, active-control demo accepted when explicitly enabled, 14 compact map beliefs written, and agent-facing logs omit hidden coordinates/feature objects.
- Added the learned-control review gate in `scenario_tests/run_learned_control_review_2_5d.mjs`, with transparent Builder / Dreamer and Critic / Reality-Checker checks before any proposed routine becomes active.
- Generated `outputs/learned_control_review_2_5d/` with a readable decision log and result report.
- Activated `adaptive_low_clearance_crossing_routine_v1` in `learned_operation_controls.md` only after the review gate passed: Builder approved the transfer evidence and compact trigger shape, Critic approved false-alarm/failure handling, rollback, hidden-truth boundary, and the active-control budget.
- Updated the embodied agent tool-boundary test so `useLearnedControl("adaptive_low_clearance_crossing_routine_v1")` succeeds from the real active registry state rather than a demo override.
- The updated embodied agent tool-boundary result passes: zero overhead step contacts, 1 probe warning, 2 crouch actions, raw detail denied, active learned control accepted from registry, 10 compact map beliefs written, and agent-facing logs still omit hidden coordinates/feature objects.
