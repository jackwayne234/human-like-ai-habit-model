# Project Progress

## 2026-06-27

- Created seven architecture notes:
  - `01_sensory_streams_system.md`
  - `02_threshold_monitors.md`
  - `03_importance_gate.md`
  - `04_memory_promotion.md`
  - `05_inner_world_model.md`
  - `06_habit_builder.md`
  - `07_efficiency_enhancer_model.md`
- Completed the 30-question design interview for `01_sensory_streams_system.md`.
- Recorded the current `01` target: create 10 Markdown sensory sample sheets in `sensory_stream_samples/`.
- Locked the first sample-sheet shape: 20 ticks per sheet, five values per tick: `brightness`, `volume`, `touch`, `taste`, and `smell`.
- Clarified the `01` boundary: the sensory stream system only emits/clamps temporary input and exposes the current rolling buffer; threshold, importance, and memory decisions belong to later layers.
- Completed the 30-question design interview for `02_threshold_monitors.md`.
- Recorded the current `02` boundary: threshold monitors are deterministic trigger recorders, similar to spreadsheet formulas. They record threshold hits only; they do not decide importance, gate opening, memory storage, or emotional meaning.
- Locked the first `02` constants: `n` threshold hits when a current sensory value equals `1.0`; `n-1` rate-of-change hits when change is at least `0.5`.
- Clarified `n`: current tick only, no previous-tick comparison.
- Clarified `n-1`: detects rate of change in two ways, within the same sensor and between different sensors.
- Clarified initial `n-2`: measures acceleration of change by reading the populated `n-1` rate-of-change trigger sheet.
- Recorded the current `02` target: after the 10 sensory sample sheets exist, create one Markdown threshold trigger sheet per sensory sample sheet in `threshold_trigger_sheets/`.
- Started `03_importance_gate.md` planning, then paused before recording answers because the importance gate needs real `01` and `02` output examples first.
- Created 10 sensory sample sheets in `sensory_stream_samples/`.
- Each sensory sample sheet contains 20 ticks and five normalized values per tick: `brightness`, `volume`, `touch`, `taste`, and `smell`.
- Created 10 matching threshold trigger sheets in `threshold_trigger_sheets/`.
- The trigger sheets apply the locked `02` rules: `n` records `threshold_hit` when a value equals `1.0`; `n-1` records within-sensor and between-sensor rate-of-change hits when one-tick absolute change is at least `0.5`.
- Early result from the concrete sheets: quiet and soft samples produce no trigger rows, isolated spike samples produce a small number of clear rows, and noisy samples produce many rows. This gives `03_importance_gate.md` better evidence for deciding what to ignore, watch, promote, record, or escalate.
- Clarified that the first test does not need to be a live stream. It can be a connected workbook with separate sheets for sensory input, `n`, `n^-1`, `n^-2`, and a review/gate sandbox.
- Created `outputs/connected_spreadsheet_test/connected_sensory_threshold_gate_test.xlsx` as the first connected spreadsheet prototype.
- Reworked the connected workbook to be compact and numeric-first: the first tab is `00 Compact View`, sensory values stay `0.0` to `1.0`, and threshold outputs use `0` or `1` instead of word labels.
- Recorded an initial memory-storage idea in `04_memory_promotion.md`: the colored brain areas can map to five sensory storage pools, starting at 20 GB each for a 100 GB total, with later reallocation by the mind model based on use, need, and survival relevance.
- Recorded an initial inner-world storage idea in `05_inner_world_model.md`: the mind gets its own separate 100 GB storage budget for building an internal world model, with an initial 80% inner-world / 20% outside-world attention split.
- Populated `03_importance_gate.md` with a first-pass gate design: emergency/reflex route, attention/brain route, three adjustable duration knobs, resource pressure rules, and gate outputs.
- Expanded `04_memory_promotion.md` with promotion inputs, memory forms, storage-pressure behavior, and sensory-pool reallocation signals.
- Expanded `05_inner_world_model.md` with the mind's world-building role, possible visual/video-game-like imagination representation, decision-tuning pressures, resource body state, and relationship to the importance gate.
- Populated `06_habit_builder.md` with a first-pass habit shape, habit promotion rules, context requirements, and relationship to the efficiency enhancer.
- Populated `07_efficiency_enhancer_model.md` with mature-habit compression targets, resource-pressure behavior, and a safety rule for reopening failed shortcuts.
- Refined the gate/mind relationship: threshold constants, duration windows, route weights, attention split, storage pressure, and compression pressure should be exposed as selectable knobs/tools that the mind model can manipulate.
- Recorded the current decision pressure model: physical limits constrain available choices, while innate drives such as self-preservation, curiosity, stability, efficiency, and coherence help the mind model choose what to adjust.
- Completed a 30-question mind-model tools/knobs interview and recorded answers in the relevant architecture logs: threshold monitor knobs, importance gate controls, memory promotion tools, inner-world/resource controls, habit automation thresholds, and efficiency compression tools.
- Clarified a key design pattern from the interview: many controls should be visible to the mind model but operate homeostatically or deterministically in the background, such as sensory storage reallocation, memory/pathway strengthening and decay, and habit automation pressure.
- Decided the next implementation step should be a compact spreadsheet control-surface prototype, not n8n yet. The spreadsheet is better for seeing knobs, resource meters, mode presets, and gate formulas all at once; n8n is better later after the control logic is proven and ready to become an event workflow.
- Next work: extend the existing connected workbook with `03`/mind-control layers: editable mind knobs, resource body meters, global mode presets, and numeric gate outputs such as ignore, watch, promote, episode, emergency, and send_upward.
- Extended `outputs/connected_spreadsheet_test/connected_sensory_threshold_gate_test.xlsx` with the first compact control-surface prototype: `03 Mind Knobs`, `04 Resource Body`, `05 Gate Output`, `06 Mode Presets`, and renamed the sample-level rollup to `07 Summary`.
- The new workbook keeps formulas inspectable and numeric-first: mind knobs expose editable mode/novelty/emergency/window/resource/curiosity/attention controls, resource body tracks storage/RAM/heat against the `0.80` marker plus deterministic sensory GB reallocation, and gate output reports `ignore`, `watch`, `promote`, `episode`, `emerg`, and `up` routes as `0`/`1`.
- Completed the 20-question control-surface interview in `08_control_surface_interview.md`.
- Implemented the interview conclusions into the architecture docs: curious startup defaults, `50%` inner-world building plus `30%` curiosity default allocation, danger-mode outward attention, deterministic protective reflexes, equal per-sense emergency priority, trigger-sequence-reward habit handoff, aggressive efficiency pressure, compact routine audit trails, disuse-based access/detail decay, and homeostasis as resource-danger prevention.
- Added a future architecture note in `05_inner_world_model.md`: later passes should consider two or more cooperating mind models for internal dialogue, reflection, planning, self-checking, creativity, and decision quality.
- Reworked the connected workbook formulas around the Q20 priority: better homeostasis, habit handoff, and efficiency pressure. The regenerated workbook now includes a compact `06 Habit Efficiency` sheet, homeostasis/resource-danger fields in `04 Resource Body`, and numeric handoff/automation/audit outputs in the sample summary.
- Chose the v1 cooperative mind-model direction: two mind models, Builder / Dreamer and Critic / Reality-Checker. Inner-world building can use Scene Builder, Memory Mapper, and Future Simulator layers as tools of the Builder, not as a third full mind model yet. A later Narrator / Integrator remains a future option if two models become too limited.
- Clarified the habit model's conversation boundary: it should not join Builder/Critic dialogue as a third mind by default. It should quietly observe trigger, sequence, and reward in order, promote reliable loops deterministically, hand mature loops to efficiency, and report upward only for new candidates, risky handoffs, failures, surprises, context mismatches, or protected areas.
- Defined the desired habit/efficiency output as a learned operation control: a deterministic code-based button, dial, tool, or routine that appears on the Builder/Critic control surface after a mature habit is compressed, so the mind models can use the operation later without thinking through the whole process.
- Froze the threshold monitor as good enough for the v1 baseline: existing knobs are sufficient, the layer should stay deterministic/formula-like and inspectable, and adaptive learned thresholds should wait until false alarms, missed events, useful promotions, and gate outcomes create real learning evidence.
- Clarified the importance gate's physical implementation direction: deterministic preset global recording/attention dial positions such as calm, curious, focused, strained, danger, and recovery, selectable by the two mind models while always balancing storage, RAM/working memory, heat/compute, and power constraints.
- Limited the habit/efficiency pipeline's write power: learned controls must be proposed through `learned_operation_controls.md`, active buttons are capped at 12 for v1, new proposals are capped at 3 per review cycle, and every proposed button/dial/tool/routine must use the same structured control template before Builder / Dreamer and Critic / Reality-Checker can accept it.
- Froze the v1 executive tool surface as sufficient: Builder / Dreamer and Critic / Reality-Checker now have enough knobs, preset dials, resource meters, evidence controls, habit/efficiency pressure, limited learned operation controls, and inner-world tools to test selection behavior before adding more executive controls.
- Created `09_end_to_end_scenario_tests.md` with five walkthrough tests: isolated volume spike, cross-sense impact, repeated touch danger, dense noise under resource strain, and repeated cleanup loop becoming a proposed learned control.
- Ran the first numeric scenario test with `scenario_tests/run_scenario_01_isolated_volume_spike.mjs`; the test report is `outputs/scenario_tests/scenario_01_isolated_volume_spike_result.md`. Result: FAIL in a useful way. The gate avoids emergency routing but promotes an isolated one-sense spike into an episode because `n^-2` acceleration is too strong without cross-sense support.
- Adjusted the scenario test formula so `n^-2` acceleration can produce `watch` or `send_upward` but cannot create an episode alone. Episode now requires promotion plus cross-sense support, repeated evidence, emergency/protective relevance, or explicit Builder / Critic confirmation. The first three numeric trials now pass: isolated volume spike, cross-sense impact, and repeated touch danger.
- Clarified full per-sensor recording policy: compact threshold triggers are cheap notices, but full sensor clips are saved only when Builder / Dreamer and Critic / Reality-Checker open a recording window through the current gate preset, curiosity/emergency settings, and resource constraints. Added `sensor_recordings/` with per-sensor folders tied to the default 20 GB sensory storage pools.
- Simplified the raw recording controls to exactly five executive buttons, one per sensor: `record_brightness`, `record_volume`, `record_touch`, `record_taste`, and `record_smell`. Each button toggles raw recording for its own sensor on or off; there is no duration, budget, cost, or all-sensor recording control in v1.
