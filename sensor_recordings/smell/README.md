# Smell Recordings

Full-detail smell clips go here when the executive mind models choose to record them.

Button: `record_smell`

Current deterministic test recording:

- `experience_session_001_raw.md`: generated by `scenario_tests/run_constant_stream_experience.mjs` while `record_smell` is on.
