# Brightness Recordings

Full-detail brightness / sight clips go here when the executive mind models choose to record them.

Button: `record_brightness`

Current deterministic test recording:

- `experience_session_001_raw.md`: generated by `scenario_tests/run_constant_stream_experience.mjs` while `record_brightness` is on.
